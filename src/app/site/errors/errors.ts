import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-errors',
  imports: [RouterLink],
  templateUrl: './errors.html',
  styleUrl: './errors.css',
})
export class Errors {}
